'use client';
import { useEffect, useState } from 'react';
import Link from 'next/link';
import { getLanguage } from '@/lib/languages';
type Form={id:string;baseVerb:string;targetText:string;translation:string;tense:string;person:string;masteryLevel:number;learningLevel:number;context:string;options:string[];generated:boolean;practiceMode:'translation'|'form-to-person'|'person-to-form'};type StageAvailability={Present:boolean;'Passato prossimo':boolean;Imperfetto:boolean;Future:boolean;mixed:boolean};
const tenses=[['Present',1],['Passato prossimo',2],['Imperfetto',3],['Future',4],['mixed',5]] as const;
export default function FormsReview(){const [language,setLanguage]=useState('it'),[tense,setTense]=useState('Present'),[forms,setForms]=useState<Form[]>([]),[index,setIndex]=useState(0),[answer,setAnswer]=useState(''),[feedback,setFeedback]=useState(''),[error,setError]=useState(''),[loading,setLoading]=useState(true),[available,setAvailable]=useState<StageAvailability>({Present:false,'Passato prossimo':false,Imperfetto:false,Future:false,mixed:false});const form=forms[index];const languageInfo=getLanguage(language);useEffect(()=>{const saved=localStorage.getItem('languagerecap-learning-language');if(saved)setLanguage(saved)},[]);useEffect(()=>{if(!languageInfo?.conjugationReview){setForms([]);setAvailable({Present:false,'Passato prossimo':false,Imperfetto:false,Future:false,mixed:false});setError('Conjugation practice is not available for this language yet.');setLoading(false);return}setLoading(true);setError('');fetch(`/api/words/forms-review?language=${encodeURIComponent(language)}&tense=${encodeURIComponent(tense)}`,{cache:'no-store'}).then(async response=>{const data=await response.json();if(!response.ok)throw new Error(data.error||'Could not load the forms test.');return data}).then(data=>{setForms(data.forms||[]);setAvailable(data.unlockedStages||available);setIndex(0);setFeedback('');setAnswer('')}).catch(reason=>setError(reason instanceof Error?reason.message:'Could not load the forms test.')).finally(()=>setLoading(false))},[language,tense]);async function submit(value:string){if(!form||feedback)return;try{if(form.generated){const expected=form.practiceMode==='form-to-person'?form.person:form.targetText;setFeedback(value.trim().toLocaleLowerCase()===expected.trim().toLocaleLowerCase()?'Correct.':'Not quite.');}else{const response=await fetch('/api/words/forms-review',{method:'POST',headers:{'Content-Type':'application/json','x-present-practice':form.practiceMode==='form-to-person'?'person':'form'},body:JSON.stringify({formId:form.id,answer:value,mode:tense,language})});const data=await response.json();if(!response.ok)throw new Error(data.error||'Could not update the forms test.');setFeedback(data.isCorrect?'Correct.':'Not quite.')}}catch(error){setFeedback(error instanceof Error?error.message:'Could not update the forms test.');return}setTimeout(()=>{setFeedback('');setAnswer('');setIndex(value=>value+1)},700)}return <main className="shell grid-paper min-h-screen"><div className="mx-auto max-w-3xl px-5 py-8 md:px-10"><header className="flex items-center justify-between border-b border-[#173c3b22] pb-6"><Link href="/learn" className="serif text-2xl font-bold">LanguageRecap</Link><Link href="/learn" className="text-sm font-bold text-[#6f7e76]">← Back to dashboard</Link></header><div className="mx-auto max-w-xl py-12"><span className="pill bg-[#e5b94e44]">{languageInfo?.conjugationReview?'Verb forms':'Forms'} · {language.toUpperCase()}</span><h1 className="serif mt-4 text-5xl font-bold">Master the details.</h1><p className="mt-3 text-[#6f7e76]">The base verb unlocks tense practice progressively.</p>{error?<section className="panel mt-8 p-8 text-center"><h2 className="serif text-3xl font-bold">Forms practice unavailable.</h2><p className="mt-2 text-[#6f7e76]">{error}</p></section>:<><label className="mt-8 block text-sm font-bold">Practice stage<select value={tense} onChange={event=>setTense(event.target.value)} className="mt-2 block w-full rounded-md border border-[#173c3b33] bg-[#fffaf1] px-3 py-3">{tenses.map(([name,level])=><option key={name} value={name} disabled={!available[name]}>{level===5?'Level 5: Mixed tense production':`Level ${level}: ${name}`}</option>)}</select></label>{loading?<p className="mt-10 text-[#6f7e76]">Loading forms...</p>:!form?<section className="panel mt-8 p-8 text-center"><h2 className="serif text-3xl font-bold">No forms ready.</h2><p className="mt-2 text-[#6f7e76]">Pass the previous learning stage to unlock this test.</p></section>:<section className="panel mt-8 p-7 md:p-10"><div className="flex justify-between text-xs font-bold uppercase tracking-[.16em] text-[#6f7e76]"><span>{tense==='Present'?'Presente':form.person+' · '+form.tense}</span><span>Level {form.learningLevel}</span></div>{tense==='Present'?<><div className="mb-4 text-center"><span className="inline-block rounded-full bg-[#e5b94e66] px-3 py-1 text-xs font-bold uppercase tracking-[.12em] text-[#6f7e76]">{form.practiceMode==='form-to-person'?'Pick the person':'Pick the form'}</span></div><div className="flex min-h-[12rem] flex-col items-center justify-center py-12 text-center">{form.practiceMode==='form-to-person'?<p className="serif text-5xl font-bold">{form.targetText}</p>:<><p className="serif text-3xl font-bold">{form.baseVerb}</p><p className="mt-3 text-sm font-bold uppercase tracking-[.12em] text-[#6f7e76]">{form.person}</p></>}</div></>:tense==='mixed'?<div className="py-12 text-center"><p className="text-sm font-bold text-[#6f7e76]">{form.context}</p><p className="serif mt-4 text-3xl font-bold">{form.translation}</p></div>:<p className="serif py-12 text-center text-5xl font-bold">{form.targetText}</p>}<div className="grid gap-3">{form.options.map((option,i)=><button key={option} onClick={()=>submit(option)} className="rounded-md border border-[#173c3b22] bg-[#f5f1e9] px-4 py-4 text-left font-bold hover:border-[#e56f50]">{String.fromCharCode(65+i)} <span className="ml-3">{option}</span></button>)}</div>{feedback&&<p className={`mt-5 text-center font-bold ${feedback==='Correct.'?'text-[#5b8555]':'text-[#e56f50]'}`}>{feedback}</p>}</section>}</>}</div></div></main>}
